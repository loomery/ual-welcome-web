export const metadata = {
  title: 'Studying',
};

// Placeholder — the Studying section is being built. Keep the route live so the
// primary nav has a destination; replace this with the real screen when ready.
export default function StudyingPage() {
  return (
    <article className="space-y-8">
      <div className="space-y-4">
        <h1>Studying</h1>
        <p className="text-step-1 text-ual-medium">This section is coming soon.</p>
      </div>
    </article>
  );
}
